# Immutable research run

Source: `f0907126339033f849df40e9525caa66462e744a`. See [checks](checks.json) for each exit code and [generated summary](data/summary.json).

Publication retains failures too; it is not a claim that every check passed. Raw logs and executables are workflow artifacts, not committed source.
