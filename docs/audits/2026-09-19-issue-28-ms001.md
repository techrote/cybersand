# Issue #28 / MS-001 — execution and acceptance ledger

## Intake and authority

The authoritative implementation branch is `codex/issue-28-ms001`, based on
`de332eaf8f4e70b25b097bed0c2e2a7b2aac0173` (selected MS-000 PR #40).
`micro_scenario_*.gd` / PR #36 is superseded and is not used. Unrelated #12/#26,
research and runner-conversion work is preserved.

The resumed intake found only workspace-transfer preparation at `686922f`; no
competing implementation or #28 PR existed. The successful Actions transfer run
`35411034735` delivered an exact source bundle, companion workspace commit
`6a58311982941b75f46e22109a0829e5acb83c7e`, all 18 required LFS payloads and pinned
Godot `4.7.stable.official.5b4e0cb0f`. Component SHA-256 checks, Git integrity, LFS
integrity and the clean source checkout were verified. No credentials were
exported. Temporary transfer workflows are staging aids, not product features.

Baseline Linux runtime SHA-256:
`f44360702d942386bf515a201cf72018bed1568e14061cf0e586d2e30c351492`.
This identifies the intake artifact, **not** the new observation adapter.
The unchanged harness passed 6471 assertions / 0 failures. Current shared-panel
changes subsequently passed the existing 52-assertion controller regression;
that is Linux execution of both controller paths, not browser execution.
An isolated baseline worktree at the same MS-000 merge generated 112 canonical
schema-1 definition hashes (39 catalogue entries with applicable seeds). The
new schema regression compares them rather than re-certifying new native code
with an old artifact hash.

## Checkpoint 1 — bounded native observation adapter

Implementation adds copied, exclusive-owner `CyberDemoBridge.inspect_cell` and
`inspect_statistics` values. These do not change kernels, Cell layout, policies,
capacities, ownership, dirty-state consumption, body masks or persistence.
Point queries reject out-of-world coordinates and quarantined worlds. Stored
material/state are distinguished from occupancy; masked temperature is explicitly
unavailable instead of being misreported as ambient temperature.

Statistics identify actual runtime capacities, current storage, successful-tick
counts, phase jobs and the existing chunk/temperature-field allocation counters.
They do not fabricate total heap allocations, queue occupancy, per-worker
utilization or per-cell sleeping status.

`test_microscenario_native_observation.gd` covers neutral reads, copy isolation,
coordinate/null rejection, synthetic mask semantics, successful tick identities,
failure quarantine and unchanged recovery capacities. A synthetic body mask is
an adapter unit test, not a Rapier-enabled headless scenario.

Local native-adapter execution passed **100 assertions / 0 failures** using
`Godot --headless --path godot --script res://tests/test_microscenario_native_observation.gd`
(timeout 120 seconds) on Debian 13 x86-64. The changed extension was built with
GCC 14.2.0 / binutils 2.44 using the explicit exploratory toolchain-drift override,
not a release-build waiver. The exact godot-cpp source remains
`101ae38034304346a46ea9ea84ae156d3e860496`; its static library was built by the
pinned GCC 13.3.0 / binutils 2.42 Actions job `35411576689`.
The **new local** extension SHA-256 is
`5990b590a4b6e07ef39664c0e8bd6fb8550645ae08c32504720af904d75da223`.
Current-head CI must independently rebuild/test with pinned tools; Windows or
browser execution is not implied by this Linux adapter result.

**Disposition at this checkpoint:** implementation in progress; current-head CI
and generated-world/UI acceptance still required. No Materials Laboratory readiness,
all-platform acceptance, G-final, interaction tuning or #28 closure is claimed.

## Checkpoint 1 remaining acceptance work (historical)

At the native-adapter checkpoint, the generated Materials Laboratory, Flood-Control
Puzzle, Simulation Stress Test, runtime publication and final integration were still
open. That dated statement is retained as historical checkpoint context; the later
sections below record their subsequent disposition rather than rewriting it.


## Checkpoint 2 — generated Materials Laboratory and shared workbench

**Implementation and local readiness proven; publication identified by the Lab
commit in PR #44 / #28.** The [retained evidence](ms001-2026-09-19/lab-readiness.json)
records exact script/test/native input hashes, generated GUI definition/capture,
headless results, screenshots' hashes, tests, limits and negative observations.
It is a local-delta record, not a fabricated source SHA or release-runtime waiver.
The complete source and controls are in the
[reference-pack runbook](../operations/microscenario-reference-pack.md).

| Required Lab checkpoint | Implementation and actual evidence |
|---|---|
| Same complete generated input loads and resets GUI/headlessly | `ms001-salt-water-v1.json`, generated by `interaction_fixture`; parsed through the normal JSON dialog, owner reset and native-only oracle; identical completed content/Water quantities |
| Scenario/version/seed/source/runtime/material/profile identities | Definition hash and ancestral recipe hash, declared material IDs/native catalogue authority, actual script/native fingerprints, effective transport/Water policies in common details and capture; unknown checkout SHA explicitly unavailable |
| Declared observations and results visible | All declarations including pending values, copied one-cell raw field probes, Salt/Brine/Stone/Steam/Sand counts and Water integer quantities; GUI details and retained capture asserted |
| Owner/reset/thread/failure boundaries | Existing host and immutable desktop handoff; copy-neutral native probes; invalid definitions preserve old world; schema-2 stale adapter precheck; synthetic-mask/quarantine regressions; fixed interest/no catch-up contract unchanged |
| Baseline/candidate A/B via fresh validated resets | Normal-dose/small-dose complete definitions stored in two bounded slots, run/captured at tick120, then baseline freshly reloaded at tick0; no worker profile setter or mode-specific physics |

On the fresh local adapter identified above: contract **239/0**, native query
**100/0**, Lab mode/observer/1-/4-worker regression **189/0**, headless workbench
**38/0**, existing desktop/Web-controller regression **52/0**, and actual X11
workbench **42/0** assertions/failures. Commands use pinned Godot with
`--headless --path godot --script res://tests/<name>.gd`, 180-second case timeout.
Graphical command uses `xvfb-run -a`, `--rendering-method gl_compatibility
--audio-driver Dummy --script res://tests/test_microscenario_workbench.gd`,
240-second outer timeout and optional `MS001_SCREENSHOT_DIR`.
The real viewport mouse button path, JSON load, full run, results/A-B dialogs,
reset and F8 labels/HUD were exercised; screenshots were visually inspected.

All four presets completed. At seed0 the normal Salt fixture converts 384 Salt
cells to 768 Brine; the small-dose fixture converts 192 to 384; quenching yields
384 Stone, and the three-bay fixture observes 384 Steam at tick120 and retains
all 384 Sand control cells at tick240. These are current-baseline observations,
not INT-000 kinetics or chemistry approval. Reaction Water deltas are retained
but not fabricated as scheduled sinks or a closed nonreacting ledger.

**Negative result retained:** the initial graphical test passed 39/40 assertions
but its whole-window 15-second deadline reached tick118/240 on the software
renderer. The corrected graphical-only 60-second window allowance did not change
required ticks, owner acknowledgements, native behavior or reset timeouts. The
rerun passed 42/42 including new actor/tool help assertions and four screenshots.
This is a test deadline defect, not an engine physics anomaly or target-PC result.

Documentation validation passes (64 documents, 112 claims); historical M11
validation passes. Retrieval top-five routing covers all original 32 questions
and 21 separate supplementary challenges; MS04/MS05 route at rank1. Their actual
returned readiness/A-B/inspection chunks were source-reviewed, not accepted just
from lexical scores. The repository gate correctly fails while old bundled
Linux/Windows runtime provenance does not match the new native adapter. This is
being repaired by rebuilding/publishing tested artifacts, not weakening the gate.

**Readiness disposition at Checkpoint 2:** the complete Lab source checkpoint is
independently reviewable and build-consumable on its published PR revision with a
rebuilt native adapter. It was **not merged-ready at that checkpoint** while runtime
publication and the other two pack scenarios remained pending. Linux synchronous
Web-controller testing is not real browser execution; Windows cross-compilation is
not Windows execution. The #28/#29/#30 checkpoint comments identify the exact Lab
consumable commit. #29 is not implemented here; all #18/#20/#26/#14 gates remain separate.


## Checkpoint 3 — Flood-Control Puzzle and Simulation Stress Test

The authoritative PR was reconciled with current main `5001f7f8665cf4e3bc67b3617796127c908ac368`
before completing the remaining pack. The merge introduced no runtime-source conflict;
documentation/retrieval conflicts were resolved additively so the independent #12
Stage-3 programme remains intact.

### Flood-Control Puzzle

`ms001/flood-control` uses only schema-2 presentation, ordinary setup rectangles,
one existing `erase` event at tick 20 and the existing `water_integer` observation.
Its single world-state objective is protected-zone Water at completed tick 180:
zero completes, any positive quantity fails. Seed 0 produces **5,031** integer Water
units untreated and therefore fails.

Three retained complete-definition witnesses preserve that exact condition while
changing only ordinary geometry:

- `containment`: reinforced reservoir aperture -> protected Water 0;
- `berm`: freestanding upstream barrier -> protected Water 0;
- `diversion`: deflector plus bounded side sump -> protected Water 0.

Their final content hashes are respectively `889c3e03e64dafe0`,
`3f105c0a24d63e80`, and `5d62d593332d1acd`; the untreated control is
`ff092bc3e0b99e35`. Distinct authoritative states plus the unchanged common
condition establish physically different witnesses rather than authored solution
flags. Play/Inspect/Benchmark and 1-/4-worker observer variants retain the same
untreated authoritative result. Focused regression:
`test_microscenario_flood_control.gd` **25 assertions / 0 failures**.

### Simulation Stress Test

Four named finite 180-tick profiles use existing setup/fill/erase semantics only:
`water-flow`, `granular-collapse`, `gas-column` and `mixed`. The largest event count
is 14, below the unchanged 24-event cap. One-worker instrumented seed-0 runs report
visited-cell sums of **2,111,536**, **9,815,552**, **6,117,120**, and **9,491,522**
respectively. These identify workloads; they are not target-PC performance thresholds.

For each profile, four-worker observer-off Play matches one-worker instrumented
Benchmark authoritative content/Water outcomes. Observer-off captures contain no
optional observation/work/timing payloads. Available counters are the existing
native visited/moved/scheduled/allocation/deferred-event fields; queue high-water,
whole-heap allocations and per-worker utilization remain explicitly unavailable.
Focused regression: `test_microscenario_stress.gd` **50 assertions / 0 failures**.

The shared contract/legacy-hash regression expands over the full pack and passes
**339 assertions / 0 failures**. The Materials Laboratory regression remains scoped
to its four material presets and passes **189 / 0**; the untreated Flood failure is
therefore not incorrectly treated as a Lab failure. The preserved shared MS-000
harness passes **6,498 / 0**, existing controller coverage **52 / 0**, native
observation **100 / 0**, and the native C++ suite **61 tests passed** on the integrated
tree. One deliberately over-parallel local sweep caused the headless workbench's
15-second fixture wait to expire once; isolated rerun passed 38/0 and the separate
X11 run passed 42/0. The contended timeout is retained as test-environment evidence,
not target-PC performance or an engine failure.

## Checkpoint 4 — interactive lifecycle repair and pack UI

Independent review reproduced three apparatus defects despite the earlier workbench
suite passing: the Definition JSON modal could leave bounded stepping active; a
same-definition accepted reset could leave a stale run waiter that timed out; and
F8 clean view could have optional telemetry restored by the next status refresh.
They were repaired at controller/workbench boundaries without touching native
simulation semantics:

- opening Definition JSON cancels further bounded-run scheduling before pausing;
- every accepted desktop or synchronous-Web MicroScenario reset cancels prior run
  state regardless of whether the definition hash changes;
- status refresh respects `microscenario_hud_hidden`, including optional telemetry.

`test_microscenario_workbench_lifecycle.gd` uses the real asynchronous desktop owner
and synchronous Web owner and passes **15 assertions / 0 failures**. It allows at
most the one already-admitted desktop step after modal entry and verifies reset
clears both the run target and acknowledgement waiter.

`test_microscenario_reference_pack_ui.gd` loads untreated Flood in Play and mixed
Stress in Benchmark through the real desktop controller, checks labelled regions,
tools/guidance, bounded-run completion/failure semantics and Stress capture telemetry;
its headless controller path passes **17 assertions / 0 failures** and its X11/Mesa
software-rendered path passes **19 / 0** with Flood/Stress screenshots. The repaired
Materials-Lab workbench passes **38 / 0** headlessly and **42 / 0** under X11/Mesa;
its clean-view screenshot was inspected with labels/help/telemetry absent. Software
rendering is UI evidence, not hardware-GPU acceptance.

## Runtime/provenance and platform disposition

The old retained Linux/Windows binaries correctly failed the repository provenance
gate once `cyber_demo_bridge.hpp` gained the read-only observation API. They are
replaced in the final candidate by pinned builds that include both changed native
inputs, including `cyber_observation_values.hpp`; the gate is not weakened.

- Linux pinned CI artifact: SHA-256
  `17b3053d9f1f7d9ee1324bd00d54421df2518c6d239f4b5b8d33b4f06651fe79`,
  workflow `35414442819`, artifact `10575133085`. This build ran Linux Godot
  regression/ABI checks for its source checkpoint; the final GDScript-only pack
  changes are revalidated separately on the same native API.
- Windows pinned cross-build: SHA-256
  `f1030f9c9d89ee392715f21cfbcd41cabded623bbd2df81ada337e9ce5b494b0`,
  workflow `35413451506`, artifact `10575336645`, LLVM-MinGW 20260826. This is
  **cross-compilation evidence only**, not Windows execution.

Actual Windows execution, real browser/WebAssembly execution and target hardware-GPU
performance/visual acceptance remain unavailable unless separately recorded by a
fresh final-platform run. Linux execution of the synchronous Web controller is not
relabeled as browser coverage. These unavailable platforms limit platform claims;
they do not change the deterministic exploratory scenario definitions or waive any
repository-required current-head check.

## Full-scope acceptance mapping before publication

| Issue #28 acceptance | Current implementation/evidence |
|---|---|
| All three scenarios share host/schema and modes | Schema 2 reference pack through the same `CyberMicroScenarioHost`; mode parity regressions retained |
| Materials Laboratory readiness | Checkpoint 2 complete definition/headless/UI/identity/fresh-reset A/B evidence |
| Flood several physical solutions | Wet untreated control plus containment/berm/diversion under one Water objective; no solution hook |
| Repeatable Stress profiles/telemetry | Four finite named profiles; unchanged caps; 1/4-worker observer-neutral semantic comparisons |
| Observer-off authoritative neutrality | Lab/Flood/Stress comparisons retain authoritative native semantics while optional records are absent |
| Generic APIs / no physics retune | No new event kind, scenario callback, kernel/default/capacity change; only read-only inspection and presentation/workbench apparatus |
| Ownership/failure/boundedness/persistence | Existing host/reset/quarantine contracts retained; new lifecycle regression covers modal/reset cancellation; capture remains non-replay |
| Explicit exploratory identity | Complete definition/source/profile/runtime identities retained; results remain exploratory against named baselines |
| Documentation/retrieval synchronization | Canonical runbook/programme/roadmap/retrieval routes updated with dated evidence and platform limits |

Final merge/issue closure remains conditional on publishing this integrated tree,
removing all temporary MS-001 transport workflows, passing repository/docs/M11/
retrieval/runtime checks on the published head, reviewing the final diff, and
verifying that the merge lands on current main.
