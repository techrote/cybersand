# Issue #28 / MS-001 — execution and acceptance ledger

## Intake and authority

The authoritative implementation branch is `codex/issue-28-ms001`, based on
`de332eaf8f4e70b25b097bed0c2e2a7b2aac0173` (selected MS-000 PR #40).
`micro_scenario_*.gd` / PR #36 is superseded and is not used. Unrelated #12/#26,
research and runner-conversion work is preserved.

The resumed intake found only workspace-transfer preparation at `686922f`; no
competing implementation or #28 PR existed. The successful Actions transfer run
`35411034735` delivered an exact source bundle, companion workspace commit
`6a58311982941b75f46e22109a0829e5acb83c7e`, all 18 required LFS payloads and pinned
Godot `4.7.stable.official.5b4e0cb0f`. Component SHA-256 checks, Git integrity, LFS
integrity and the clean source checkout were verified. No credentials were
exported. Temporary transfer workflows are staging aids, not product features.

Baseline Linux runtime SHA-256:
`f44360702d942386bf515a201cf72018bed1568e14061cf0e586d2e30c351492`.
This identifies the intake artifact, **not** the new observation adapter.
The unchanged harness passed 6471 assertions / 0 failures. Current shared-panel
changes subsequently passed the existing 52-assertion controller regression;
that is Linux execution of both controller paths, not browser execution.
An isolated baseline worktree at the same MS-000 merge generated 112 canonical
schema-1 definition hashes (39 catalogue entries with applicable seeds). The
new schema regression compares them rather than re-certifying new native code
with an old artifact hash.

## Checkpoint 1 — bounded native observation adapter

Implementation adds copied, exclusive-owner `CyberDemoBridge.inspect_cell` and
`inspect_statistics` values. These do not change kernels, Cell layout, policies,
capacities, ownership, dirty-state consumption, body masks or persistence.
Point queries reject out-of-world coordinates and quarantined worlds. Stored
material/state are distinguished from occupancy; masked temperature is explicitly
unavailable instead of being misreported as ambient temperature.

Statistics identify actual runtime capacities, current storage, successful-tick
counts, phase jobs and the existing chunk/temperature-field allocation counters.
They do not fabricate total heap allocations, queue occupancy, per-worker
utilization or per-cell sleeping status.

`test_microscenario_native_observation.gd` covers neutral reads, copy isolation,
coordinate/null rejection, synthetic mask semantics, successful tick identities,
failure quarantine and unchanged recovery capacities. A synthetic body mask is
an adapter unit test, not a Rapier-enabled headless scenario.

Local native-adapter execution passed **100 assertions / 0 failures** using
`Godot --headless --path godot --script res://tests/test_microscenario_native_observation.gd`
(timeout 120 seconds) on Debian 13 x86-64. The changed extension was built with
GCC 14.2.0 / binutils 2.44 using the explicit exploratory toolchain-drift override,
not a release-build waiver. The exact godot-cpp source remains
`101ae38034304346a46ea9ea84ae156d3e860496`; its static library was built by the
pinned GCC 13.3.0 / binutils 2.42 Actions job `35411576689`.
The **new local** extension SHA-256 is
`5990b590a4b6e07ef39664c0e8bd6fb8550645ae08c32504720af904d75da223`.
Current-head CI must independently rebuild/test with pinned tools; Windows or
browser execution is not implied by this Linux adapter result.

**Disposition at this checkpoint:** implementation in progress; current-head CI
and generated-world/UI acceptance still required. No Materials Laboratory readiness,
all-platform acceptance, G-final, interaction tuning or #28 closure is claimed.

## Remaining acceptance work

Finish and evidence the generated Materials Laboratory, shared identities/results
and fresh-reset A/B controls; publish its exact consumable revision independently.
Then complete and demonstrate Flood-Control Puzzle solutions and bounded named
Stress Test profiles. The final evidence must map all acceptance criteria,
retain negative results, identify fresh runtime/source hashes, distinguish Linux
controller tests from Windows/browser execution, and synchronize the canonical
programme, controls, roadmap, retrieval routes and issue mirrors.
