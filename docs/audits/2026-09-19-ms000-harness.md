---
title: MS-000 extraction checkpoint and scoped evidence
status: Current
document-kind: evidence
scope: Issue 27 harness plus isolated issue 24 prerequisite; Linux native/owner validation and explicit platform gaps
canonical-for: []
last-reviewed: 2026-09-19
related-documents: [../operations/micro-scenario-harness.md, ../operations/microscenarios-programme.md, ../reference/validation-evidence.md]
---

# MS-000 extraction checkpoint, 2026-09-19

## Source and runtime identities

Implementation starts at `781a2d7cb5d5a66bf1cff8fc6654e0ab708e419c` (main),
not draft PR #25 or #33. Branch: `codex/issue-27-microscenarios-harness`.
The isolated local #24 prerequisite commit is
`b64fd4f` (launcher focus plus test); source changes after that checkpoint are
identified by [the tested source manifest](2026-09-19-ms000-source.json).
Temporary source/toolkit acquisition workflows are removed from the product diff.
Native engine, binding, material catalogue, Water solver and profile defaults are
unchanged. All 18 retained Linux native `source_inputs` match the current sources.

Runtime: Linux x86_64, Godot `4.7.stable.official.5b4e0cb0f`, Rapier2D `0.35.2`.
Pinned Godot ZIP SHA-256:
`0b1a6c54c2c619c12e169fe9241edda4b81080b519451cec2984bf0d2c6cb73c`.
Actual retained native extension SHA-256:
`f44360702d942386bf515a201cf72018bed1568e14061cf0e586d2e30c351492`.
The native manifest's source commit is
`b66fce413ba83ec76ffab303b3bb293cc20e1d53`; it is not falsely relabelled as the
new GDScript source commit. Toolkit acquisition run `35405498169` and source
acquisition run `35405311000` are not runtime acceptance tests.

## Focused execution results

Each focused invocation used pinned Godot `--headless --path godot --script
res://tests/<name>.gd`, with a 70-second outer timeout. Results:

| Fixture | Result and comparison |
|---|---|
| `test_tower_launcher` | PASS: launcher is non-focusable, opens Tower and does not keep keyboard focus. |
| `test_micro_scenario_contract` | PASS: 37 definitions validate; JSON round-trip hashes agree; invalid IDs/seeds/profiles/regions/events/conditions/capacities reject. |
| `test_micro_scenario_runtime` | PASS: independent old native builder vs host Tower setup, plus all 35 Water recipes at mass3/mass8, coherence7, seed17. Exact initial observations/hashes agree. |
| Generic parity matrix | PASS: communicating reservoirs, seed17, 121 ticks, three modes × observers on/off × workers1/4; identical authoritative hashes and Water quantity; finite event/objective behavior preserved. |
| `test_micro_scenario_controllers` | PASS: real desktop owner acknowledgement, single-step/reset, immutable captures, allowed tools, HUD and legacy exit; synchronous Web controller parity, reset, step, save rejection and owner-flag restoration. |
| Failed-world cases | PASS: actual native capacity failure remains quarantined after invalid reset; explicit fresh replacement recovers; desktop diagnostic acknowledgement does not retry the failed tick or mutate prior physical publications. |
| Existing Water scenarios/controller | PASS: original catalogue hash, transactional reset, blind workflow and body activation tests remain green. |

Normalized metadata deliberately differs: the new schema/definition hash is
additional identity, while original Tower rectangles, Water recipe bytes/actions,
recipe versions and legacy hashes remain controls. The unchanged 35-recipe
catalogue hash is
`c0d465a25dfd0bf26301c1f2466b327c8d9af914e6254d9defff07a9bf662014`.

The new closed fixture's parity result at tick121 is content hash
`92bdac109022b3b4`, state hash `c6f9e5f3f206a0dc`, Water integer quantity
`6858480`, and `26938` Water cells. This proves the stated fixture comparisons,
not full replay, physical realism, pressure convergence or human preference.

The standalone CLI also passed using `res://tools/micro_scenario_benchmark.gd --
--scenario=communicating-reservoirs --seed=17 --ticks=121 --workers=4
--mode=benchmark --observers=on`. It exported a successful 121-tick report with
definition hash `872d09b696657e2f4533b112c7be048a6ce0f5effcde24d070c34955b6c7f782`.
Its concurrent-host timing is not presented as performance acceptance.

## Full checks

**PASS: all 38 Godot invocations**, including every current test, both profiles and
the scene. **PASS: 61 native tests** from `timeout -k 2s 180s make test`.
M11 historical consistency and validation-failure unit tests also pass.
The frozen 32-query retrieval set hits all 32 canonical owners in the first five
results (21 at rank one); the supplementary set hits 18/18 (13 at rank one).
The two new MS-000 challenges are disclosed separately, with manual review of
owner-ordering and incomplete-replay qualifiers. No before/after improvement is claimed.

Initial local documentation/repository checks found six missing references to
the separate pinned companion workspace. The exact referenced files from
`6a58311982941b75f46e22109a0829e5acb83c7e` were then materialized via acquisition
run `35407209660`. Both `python tools/ci/check_docs.py` and
`python tools/ci/check_repository.py` subsequently passed. The stricter local
`--require-materialized` check still rejects nine non-Linux runtime objects,
which remain LFS pointers in this Linux-only checkout. The PR workflow retrieves
all LFS objects; its strict CI result remains a separate gate, not assumed here.

The Godot runner discovers all `test_*.gd`, imports once, executes both existing
native profiles and opens the scene. Its per-test timeout is 180 seconds, profile
timeout 1200 seconds. Profiles are run serially; their timing is not a production
benchmark.

## Coverage limits and continuation

The Web-controller tests here run its synchronous GDScript owner on Linux native
Godot; they are **not a real browser/WebAssembly validation**. Fresh compatibility
and threaded native Web browser exports, Windows runtime execution and human visual
review are not claimed. Historical #19 evidence is retained, not extended to the
new source by assertion. The headless body-enabled runner refuses unsupported
Rapier omission; scene-owned body paths are tested by the retained controller tests.

The new fixture is exploratory #26-style apparatus, not evidence replacing a
registered #26 run. No #20 fixture is implemented before its own admission. #28 can
author against this contract; #29 implementation still requires the Materials
Laboratory readiness checkpoint. #18/#20/#26/#14 scientific gates do not change.

The immutable test manifest, source branch and pull request are the recoverable
checkpoint. Raw local test logs are not committed as source; PR CI retains its own
identified artifacts. Close/merge decisions must use the actual current CI status,
not the acquisition runs or this document's existence.
